# OpenGradient Developer Cookbook — Python dependencies
# Python 3.11+ is required (enforced by opengradient >= 0.6.0)
#
# Install:
#   pip install -r requirements.txt

# Core SDK — API stable since 0.9.x
opengradient>=0.9.0

# Environment variable management
python-dotenv>=1.0.0

# Blockchain / wallet dependencies (pinned by SDK transitive requirements)
web3>=7.3.0
eth-account>=0.13.4

# LLM framework integrations (optional — needed for snippets 11)
# pip install langgraph langchain-core
langchain>=0.3.7
langchain-community>=0.3.7
langchain-core>=0.3.0
